/* ==================================================================
 * smppserver-cfg.def - Definicion de la configuracion de Grupos y Variables Definition of configuration groups and variables
 *
 * Rodrigo Vallejo desarrollo@mplus.ec
 *
 */

SINGLE_GROUP(httpServerMovistarEc,
	OCTSTR(httpServer-id)
	OCTSTR(httpServer-port)
	OCTSTR(log-file)
	OCTSTR(log-level)
	OCTSTR(server-root)
	OCTSTR(user)
	OCTSTR(path)
	OCTSTR(default)
	OCTSTR(keep-alive)
	OCTSTR(time-out)
	OCTSTR(mime-types)
	OCTSTR(pid-file)
	
)



SINGLE_GROUP(mysql-connection,
    OCTSTR(id)
    OCTSTR(host)
    OCTSTR(port)
    OCTSTR(username)
    OCTSTR(password)
    OCTSTR(database)
    OCTSTR(max-connections)
)
